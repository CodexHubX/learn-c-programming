#define _CRT_SECURE_NO_WARNINGS // ปิด warning ของ Microsoft ที่บอกว่าฟังก์ชันบางตัว (เช่น scanf, fopen, strcpy) "ไม่ปลอดภัย"
#define MAX_PLAYERS 100 // ประกาศคงที่ คล้ายๆ cost

#include <stdio.h>

int main(void) {
	printf("Hellow World\n");
	printf("I will become a kernel hacker.\n");
	printf("Player : %d\n", MAX_PLAYERS);
	return 0;
}